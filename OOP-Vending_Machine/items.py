class items:
    def items(self,code,item):
        self.items[code] = item

    chips.price = float(1.50)
    candy.price = float(1.25)
    soda.price = float(1.00)

    chips.amount = int(5)
    candy.amount = int(3)
    soda.price = int(10)
    
    quarter = float(0.25)
    dime = float(0.10)
    nickel = float(0.05)
    cent = float(0.01)